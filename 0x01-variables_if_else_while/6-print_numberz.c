#include <stdio.h>
/**
 *main - entry point
 *Return: is an integer
 */
int main(void)
{
int number, newline;
number = 0;
newline = '\n'
while (number < 10)
{
putchar(number);
number++;
}
putchar(newline);
return (0);
}
