#include "holberton.h"
/**
 *_islower - check for lowercase characters
 *Return:  return 1 if lowercase, 0 if otherwise
 */
int _islower(int c)
{
  int i = 'a';
  while (i <= 'z')
    {
      if (c == i)
    }
}
